# r10k.yaml
---
sources:
  puppet:
    remote: 'https://${{ secrets.GITHUB_TOKEN }}@github.com/batchayw/puppet-project.git'
    basedir: '/etc/puppetlabs/code/environments'
    environments:
      - production